# Environment variables — NEVER commit these
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-electron
release
*.local
*.ipa
ios_certificates/
docx_extract.txt
pdf_extract.txt
workbook_utf8.md
upload_to_apple.bat

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ─── Added: keep secrets, builds, and manuscripts out of git ───
# Signing keys — NEVER commit. See SECURITY-CLEANUP.md
*.keystore
*.jks
*.p12
*.mobileprovision

# Build artifacts — attach to GitHub Releases instead
*.apk
*.aab
*.ipa
appflow.exe
release/
dist-electron/

# Manuscript source files — keep in cloud storage, not the app repo
Copy of The Resilient Path*.docx
The_resilient_path_workbook*.docx
Managing Life with Chronic Pain*.pdf
Managing Life with Chronic Pain*.jpeg

# Local env (holds VITE_PROXY_URL / VITE_APP_TOKEN)
.env
.env.*
!.env.example
